<tr class="<%= class_tr_message(conversation) %>">
  <td><%= format_participants(mailbox_type, conversation) %> (<%= conversation.count_messages %>)</td>
  <td><%= link_to(conversation.subject, conversation_path(conversation)) %></td>
  <td class="text-right"><%= l(conversation.last_message.created_at, :format => :default) %></td>
</tr>
