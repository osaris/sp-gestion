<div class="btn-toolbar pull-right">
  <div class="btn-group">
    <%= link_to(icon_label_text('glyphicon glyphicon-plus', 'Nouveau message'),
                new_conversation_path,
                :class => 'btn btn-default btn-sm') %>
  </div>
</div>

<br class="clearfix" />

<%= render_navigation(:context => :conversations) %>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Messages</th>
      <th>Date</th>
      <th>Expéditeur</th>
      <th>Sujet</th>
    </tr>
  </thead>
  <tbody>
    <%= render(:partial => 'conversation', :collection => @conversations) || raw("<tr><td colspan='4'>Pas encore de conversation !</td></tr>") %>
  </tbody>
</table>
<%= page_navigation_links(@conversations) %>
