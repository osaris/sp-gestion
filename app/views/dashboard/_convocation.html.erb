<tr class="<%= cycle("odd", "even") %>">
  <td><%= l(convocation.created_at, :format => :default) %></td>
  <td><%= link_to(h(convocation.title), convocation) %></td>
  <td><%= h(convocation.place) %></td>
</tr>