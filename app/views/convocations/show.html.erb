<h1><%= @convocation.title %> (<%= l(@convocation.date) %>)</h1>
<p>Lieu : <%= @convocation.place %></p>
<p>Tenue : <%= @convocation.uniform.description %></p>
<%- unless @convocation.rem.blank? -%>
  <p>Remarque : <%= @convocation.rem %></p>
<%- end -%>
<%- unless @convocation.last_emailed_at.blank? -%>
  <p>Envoyée par email le <%= l(@convocation.last_emailed_at, :format => :default)%>
<%- end -%>
<p>
  <%= link_to(image_tag("back/icons/edit.png", :alt => "Editer") + raw("&nbsp;Editer"), edit_convocation_path(@convocation))%> -
  <%= link_to(image_tag("back/icons/delete.png", :alt => "Supprimer") + raw("&nbsp;Supprimer"), convocation_path(@convocation), :method => :delete, :confirm => "Êtes-vous sûr ?")%> -
  <%= link_to(image_tag("back/icons/print.png", :alt => "Imprimer les convocations") + raw("&nbsp;Imprimer les convocations"), convocation_path(@convocation, :format => :pdf))%> -
  <%= link_to(image_tag("back/icons/email.png", :alt => "Envoyer par email") + raw("&nbsp;Envoyer par email"), email_convocation_path(@convocation), :method => :post, :confirm => "Seuls les personnes ayant une adresse email recevront la convocation. Êtes-vous sûr ?")%> -
  <%= link_to(image_tag("back/icons/user.png", :alt => "Présences") + raw("&nbsp;Présences"), edit_all_convocation_convocation_firemen_path(@convocation))%>
</p>
<h2>Convoqués</h2>
<table>
  <thead>
    <tr>
      <th>Grade</th>
      <th>Nom</th>
      <th>Prénom</th>
      <th>Présent ?</th>
    </tr>
  </thead>
  <tbody>
    <%-@convocation.convocation_firemen.group_by(&:status).each do |group, convocation_firemen| -%>
      <tr>
        <th colspan="4">
          <%= Fireman::STATUS.index(group) %>&nbsp;-&nbsp;<%= display_stats_presence(group) %>
        </th>
      </tr>
      <%= render(convocation_firemen) %>
    <%- end -%>
  </tbody>
</table>