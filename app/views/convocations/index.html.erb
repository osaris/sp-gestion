<div class="btn-toolbar pull-right">
  <div class="btn-group">
    <%= link_to(icon_label_text('glyphicon glyphicon-plus', 'Créer une nouvelle convocation'),
                new_convocation_path,
                :class => 'btn btn-default btn-sm') %>
  </div>
</div>
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Date</th>
      <th>Titre</th>
      <th>Lieu</th>
      <th>Tenue</th>
      <th>Dern. envoi email</th>
  </thead>
  <tbody>
    <%= render(:partial => @convocations) || raw("<tr><td colspan='5'>Pas encore de convocation !</td></tr>") %>
  </tbody>
</table>
<%= page_navigation_links(@convocations) %>