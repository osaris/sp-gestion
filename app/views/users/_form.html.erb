<p class="alert alert-info">Les champs marqués d'une astérisque (*) sont obligatoires. Un email sera envoyé à l'utilisateur avec un lien pour confirmer son compte et choisir un mot de passe.</p>
<%= form_for(user, :html => { :class => 'form-horizontal' }) do |f| %>
  <fieldset>
    <div class="control-group">
      <%= f.label(:email, "Adresse email", :class => 'control-label') %>
      <div class="controls">
        <%= f.text_field(:email) %>
        <%= error_message_on(:user, :email) %>
      </div>
    </div>
    <div class="control-group">
      <div class="controls">
        <%= submit_tag("Enregistrer", :class => 'btn btn-primary btn-large') %> ou <%= link_to("Annuler", users_path) %>
      </div>
    </div>
  </fieldset>
<%- end -%>