<p class="alert alert-warning">Les utilisateurs n'appartenant pas à un groupe ont tous les droits.</p>
<div class="btn-toolbar pull-right">
  <div class="btn-group">
    <%= link_to(icon_label_text('glyphicon glyphicon-plus', 'Créer un nouvel utilisateur'),
                new_user_path,
                :class => 'btn btn-default btn-sm') %>
  </div>
</div>
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Adresse email</th>
      <th>Groupe</th>
      <th>Dernière connexion</th>
      <th>Remarque</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <%= render(@users) %>
  </tbody>
</table>
<%= page_navigation_links(@users) %>
<p><%= render('users/owner_instructions') %><p>
