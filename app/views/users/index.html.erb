<div class="btn-toolbar pull-right">
  <div class="btn-group">
    <%= link_to(icon_label_text('icon-plus', 'Créer un nouvel utilisateur'),
                new_user_path,
                :class => 'btn btn-small') %>
  </div>
</div>
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Adresse email</th>
      <th>Groupe</th>
      <th>Remarque</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <%= render(@users) %>
  </tbody>
</table>
<%= page_navigation_links(@users) %>
<p><%= render('users/owner_instructions') %><p>