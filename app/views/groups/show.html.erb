<div class="block-title"><h4><%= @group.name %></h4></div>
<div class="clearfix"></div>
<div class="btn-toolbar pull-left">
  <div class="btn-group">
    <%= link_to(icon_label_text('glyphicon glyphicon-edit', 'Editer'),
                edit_group_path(@group),
                :class => 'btn btn-default btn-sm') %>
    <%= link_to(icon_label_text('glyphicon glyphicon-remove', 'Supprimer'),
                group_path(@group),
                :method => :delete, :data => { :confirm => "Êtes-vous sûr ?" },
                :class => 'btn btn-default btn-sm') %>
  </div>
</div>
<div class="clearfix"></div>
<div class="block-title"><h5>Utilisateurs</h5></div>
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Email</th>
    </tr>
  </thead>
  <tbody>
    <%= render(:partial => 'user', :collection => @group.users) || raw("<tr><td colspan='1'>Pas encore d'utilisateur !</td></tr>") %>
  </tbody>
</table>
<div class="block-title"><h5>Droits</h5></div>
<%= render(:partial => 'group_table', locals: {:group => @group, :resources => @resources}) %>
