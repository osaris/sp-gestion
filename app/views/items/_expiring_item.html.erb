<tr>
  <td><%= link_to(h(expiring_item.title), edit_check_list_item_path(expiring_item.check_list, expiring_item)) %></td>
  <td><%= link_to(h(expiring_item.check_list.title), check_list_path(expiring_item.check_list)) %></td>
  <td><%= expiring_item.place %></td>
  <td><%= expiring_item.quantity %></td>
  <td><%= l(expiring_item.expiry) %></td>
</tr>