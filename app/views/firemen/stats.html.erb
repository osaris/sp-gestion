<div class="block-title stats"><h4>
  Statistiques pour le <%= link_to(grade_and_name(@fireman), @fireman) %> pour l'année
  <%= form_tag(firemen_stats_change_year_path(@fireman, params[:type]),
               :method => :get,
               :id => :form_stats_firemen,
               :style => "display:inline;") do -%>
    <%= select_tag(:new_year, options_for_select(@years_stats, params[:year])) %>
  <%- end -%>
</h4></div>

<%= render_navigation(:context => :firemen_stats,
                      :level => 1) %>

<%= render("firemen/stats/#{params[:type]}", :data => @data, :nb_interventions => @nb_interventions) %>
