<div class="btn-toolbar pull-right">
  <div class="btn-group">
    <%= link_to(icon_label_text('glyphicon glyphicon-plus', 'Créer une nouvelle entrée'),
                new_daybook_path,
                :class => 'btn btn-default btn-sm') %>
  </div>
</div>
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Date</th>
      <th>Affiché sur le standard</th>
      <th>Texte</th>
    </tr>
  </thead>
  <tbody>
    <%= render(:partial => @daybooks) || raw("<tr><td colspan='3'>Pas encore d'entrée !</td></tr>") %>
  </tbody>
</table>
<%= page_navigation_links(@daybooks) %>
